style
  include checkbox.scss
// mode - text, masked, subscription
mixin checkbox(lab, mode)
  div.checkbox
    label.checkbox__label #{lab}
      input(type="checkbox" class="checkbox__input"+" checkbox__input-"+mode)
      span.checkbox__mark  
    